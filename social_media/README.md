# social_media (bundle payload)

This directory is shipped **inside** `CTI_Pipeline_Win.exe` via PyInstaller `--add-data`.

Replace or extend with your Brand Scout / social pipeline assets (e.g. `docker-run.sh`, compose files) when you wire the full stack. The pipeline CLI prints its resolved path via `CTI_Pipeline_Win.exe info`.
